import { Section } from "../shared/Section";
import { MissionTitle } from "./MissionTitle";

export const MissionSection = () => {
    return (
        <Section>
            <MissionTitle />
        </Section>
    );
};
