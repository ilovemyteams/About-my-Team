import { Section } from "../shared/Section";
import { Title } from "./Title";

export const ServicesSection = () => {
    return (
        <Section>
            <Title />
        </Section>
    );
};
